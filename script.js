function newItem(name) {
    return new item(name)
}

function sceneClick() {
    items["scenes"].add(items["scenes"].getClickAmount()) 
}

function modulesClick() {
    items["Peaceful Scenes"].add(items["Peaceful Scenes"].getClickAmount())
}